"""
Imprnt AI — Compositor: Puppeteer Renderer
Phase 1: Core implementation.

render_poster(html, width, height, output_path) → PNG file path

Uses pyppeteer. Falls back to Satori if pyppeteer fails to install.
waitUntil='networkidle0' ensures Google Fonts finish loading before screenshot.
"""
# Full implementation in Phase 1.
