"""
Imprnt AI — Compositor: Satori Fallback
Used if pyppeteer fails to install on macOS.
Full implementation in Phase 1 (only if Puppeteer fails).
"""
# Fallback implementation — only activated if Puppeteer fails.
